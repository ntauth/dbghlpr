//
// comments
//
	// ver.0.1.
	+ added code analyzer

	// ver.0.2.
	+ 재귀식 분석 동작에서 루틴 분석 방식으로 변경

	// ver.0.3.
	+ bug-fix

	// ver.0.4.
	+ added x64 build

	// ver.0.5.
	+ added xref to func

	// ver.0.6.
	+ bug-fix
		- xref가 현재 재귀 호출로 수행되는데, 이 때 호출되는 리스트가 길어지면 오버플로가 발생
		- xref 기능 버튼 누르도록 수정

	// ver.0.7.
	+ 블럭 단위를 PE 이미지가 아닌, segment 단위로 변경.

	// ver.0.8.
	+ 코드 분석 동작이 방문한 주소는 다시 참조하지 않음 따라서 동일한 영역을 참조하는 코드들의 경우,
	트레이스 기능을 사용하면 하단이 짤리는 경우가 발생. 따라서, 1회 엔트리 분석 이후 다시 재분석하도록 변경

	// ver.0.9.
	+ 명령어 단순화 작업

	// ver.1.0.
	+ 코드 정리
	+ 분석된 블럭 기준으로 병합 및 나눌 수 있도록 수정
		- 사용자 정의 기능
	+ 함수 진입점에 tag 하여 바로가기 기능 추가
	+ 분석된 어셈블리 라인에 대해 코멘트 입력 가능하도록 수정 
	+ 에뮬레이팅 기능 추가 
	+ 세그먼트 개념 제거
